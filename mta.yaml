# _schema-version: 3.3.0
# ID: simplecap
# version: 1.0.0

# parameters:
#   enable-parallel-deployments: true

# build-parameters:
#   before-all:
#     - builder: custom
#       commands:
#         - npm ci
#         - npx cds build --production

# modules:
#   # CAP Service
#   - name: simplecap-srv
#     type: nodejs
#     path: gen/srv
#     parameters:
#       instances: 1
#     provides:
#       - name: srv-api
#         properties:
#           srv-url: ${default-url}
#     requires:
#       - name: simplecap-auth
#       - name: simplecap-db

#   # DB Deployer
#   - name: simplecap-db-deployer
#     type: hdb
#     path: gen/db
#     requires:
#       - name: simplecap-db

#   # SUBACCOUNT Destination Content (IMPORTANT)
#   - name: simplecap-destination-content
#     type: com.sap.application.content
#     requires:
#       - name: simplecap-destination
#         parameters:
#           content-target: true
#       - name: srv-api
#       - name: simplecap-auth
#     parameters:
#       content:
#         subaccount:
#           destinations:
#             - Name: simplecap-srv
#               URL: ~{srv-api/srv-url}
#               Authentication: OAuth2UserTokenExchange
#               ServiceInstanceName: simplecap-auth
#               ForwardAuthToken: true
#               sap.cloud.service: simplecap
#               HTML5.DynamicDestination: true
#               WebIDEEnabled: true
#               WebIDEUsage: odata_gen

#           existing_destinations_policy: update
#     build-parameters:
#       no-source: true
# resources:
#   # XSUAA
#   - name: simplecap-auth
#     type: org.cloudfoundry.managed-service
#     parameters:
#       service: xsuaa
#       service-plan: application
#       path: ./xs-security.json

#   # HANA
#   - name: simplecap-db
#     type: com.sap.xs.hdi-container
#     parameters:
#       service: hana
#       service-plan: hdi-shared

#   # Destination service (carrier only)
#   - name: simplecap-destination
#     type: org.cloudfoundry.managed-service
#     parameters:
#       service: destination
#       service-plan: lite

_schema-version: 3.3.0
ID: simplecap
version: 1.0.0
description: "CAP project with instance-level destination"

parameters:
  enable-parallel-deployments: true

build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production

modules:
  # ---------------- CAP Service ----------------
  - name: simplecap-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api
        properties:
          srv-url: ${default-url}
    requires:
      - name: simplecap-auth
      - name: simplecap-db

  # ---------------- DB Deployer ----------------
  - name: simplecap-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: simplecap-db

  # ---------------- Destination Content (INSTANCE LEVEL) ----------------
  - name: simplecap-destination-content
    type: com.sap.application.content
    requires:
      - name: simplecap-destination
        parameters:
          content-target: true
      - name: srv-api
      - name: app-api
      - name: simplecap-auth
    parameters:
      content:
        instance:
          destinations:
            - Name: simplecap-srv
              URL: ~{srv-api/srv-url}
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: simplecap-auth
              sap.cloud.service: simplecap
              HTML5.DynamicDestination: true
              WebIDEEnabled: true
              WebIDEUsage: odata_gen

            - Name: simplecap-approuter
              URL: https://~{app-api/app-uri}
              # URL: ~{app-api/app-protocol}://~{app-api/app-uri}
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: simplecap-auth
              sap.cloud.service: simplecap
              HTML5.DynamicDestination: true
              WebIDEEnabled: true
              WebIDEUsage: odata_gen
          existing_destinations_policy: update
    build-parameters:
      no-source: true
  - name: simplecap
    type: approuter.nodejs
    path: app/router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: simplecap-auth
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}

resources:
  # ---------------- XSUAA ----------------
  - name: simplecap-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      service-name: simplecap-auth
      path: ./xs-security.json
      config:
        xsappname: simplecap-xsapp
        tenant-mode: dedicated
        oauth2-configuration:
          redirect-uris:
            - https://*~{app-api/app-uri}/**
    requires:
      - name: app-api

  # ---------------- HANA HDI ----------------
  - name: simplecap-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared

  # ---------------- Destination Service ----------------
  - name: simplecap-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      service-name: simplecap-destination
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            destinations:
              - Name: ui5
                Type: HTTP
                ProxyType: Internet
                Authentication: NoAuthentication
                URL: https://ui5.sap.com
            existing_destinations_policy: update
        version: 1.0.0
